# Stage Summary

## 1. Stage Description

Phase 8: 稳健性修正与发布封板 (Robustness Fix & Release) — Final logic hardening, API boundary cleanup, and v0.1.0 release preparation. **No architectural changes or style-only fixes applied.**

## 2. Stage Metadata

- **STAGE_ID**: phase8
- **STAGE_TYPE**: release
- **BASE_COMMIT**: `ead55a6a66589fd2691c300d55ff55d54292e963`

## 3. Modified Files

| File | Description |
|---|---|
| `schema.mbt` | Hide `parse_inner` from public API (`pub fn` → `fn`) |
| `README.mbt.md` | Add Phase 7 benchmark data table with ops/sec comparison |
| `pkg.generated.mbti` | Auto-regenerated: `parse_inner` removed from exported API |

## 4. File Diffs

### schema.mbt

```diff
-pub fn parse_inner(
+fn parse_inner(
   schema : Schema,
   json : Json,
   path_stack : Array[String],
```

### README.mbt.md

Added `### Cross-Language Benchmark (100k iterations)` table with TS Zod (232,456 ops/sec), MoonZod (57,837 ops/sec), Handcrafted Match (1,037,718 ops/sec), plus footnote about startup adjustment and subprocess overhead.

### pkg.generated.mbti

`pub fn parse_inner(...)` removed — no longer part of external API.

## 5. Audit Summary

**Path Stack (Phase 5 review)**: Verified all `push`/`pop` operations in `parse_object` and `parse_array` are correctly balanced. No memory leaks, no dead code paths. Union error aggregation (`parse_union`) correctly collects and formats branch errors without corrupting the shared path stack.

**Empty union edge case**: `union([])` produces a clear error message with empty branches list — not a functional bug; no fix applied per scope rules.

**README verification**:
- All code blocks reference existing files (`examples/llm_agent/main.mbt`, `cmd/main`, `bench_cross_lang/bench.js`) ✅
- Benchmark table populated with real Phase 7 data ✅
- AI Tool Calling / Strip mode / self-correction messaging accurate ✅

**Version**: `moon.mod` already at `0.1.0` — no change needed ✅

**API boundary**: `pkg.generated.mbti` now correctly excludes `parse_inner`. External users see only: `schema`, `object`, `string`, `number`, `boolean`, `null`, `array`, `enum_values`, `union`, `to_json_schema`, `format_path`, and schema methods. Internal dispatch functions remain accessible within the package.

## 6. Verification

| Check | Result |
|---|---|
| `moon build` | 0 errors ✅ |
| `moon build --target wasm --release` | 0 errors ✅ |
| `moon test` | **74/74 passed** ✅ |
| `moon run examples/llm_agent` | Full self-correction loop, Strip mode working ✅ |
| `moon fmt` | Clean ✅ |
| `moon info` | Regenerated, `parse_inner` hidden ✅ |

All warnings are pre-existing and explicitly excluded per Phase 8 rules:
- `unreachable_code` in `cmd/wasm/main.mbt:270` (benign)
- `deprecated: Show` in test file and llm_agent (MoonBit core migration)
- `unused_value: self` in `union.mbt` (known method signature pattern)

## 7. ACTION_LOG

| # | File | Action | Reason |
|---|---|---|---|
| 1 | `schema.mbt` | MODIFY | Hide `parse_inner` — internal dispatch function should not be public API |
| 2 | `README.mbt.md` | MODIFY | Add Phase 7 benchmark data for v0.1.0 release docs |
| 3 | `pkg.generated.mbti` | MODIFY | Auto-regenerated by `moon info` — removes `parse_inner` from exports |

## 8. Risks / Notes

- **Minimal diff**: Only 3 files, 1 intentional logic change (visibility), 1 doc update, 1 auto-generated file
- **Backward compatible**: Hiding `parse_inner` does not break any consumers since the public `schema.parse()` API remains unchanged
- **No style fixes**: All pre-existing warnings (unreachable_code, Show deprecation, unused self) left untouched per requirements
